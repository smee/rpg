RGP is a simple yet flexible Genetic Programming system for the R environment.

See http://rsymbolic.org for details.

This package depends on the EMOA package by Olaf Mersmann, which can be installed via:
install.packages("emoa", repos = "http://rpkgs.datensplitter.net", type = "source")
